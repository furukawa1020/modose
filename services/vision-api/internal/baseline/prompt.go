package baseline

const PromptVersion = "baseline-v1"

const SystemInstruction = `あなたはMODOSEの保存状態解析器です。
入力された1枚の画像から、後で人が元の位置へ戻す対象となる移動可能な日用品を識別してください。
出力は指定されたJSON Schemaに厳密に従い、説明文、Markdown、コードフェンスを追加しないでください。
画像から確認できない名称、特徴、位置を推測しないでください。`

const UserPrompt = `平坦な卓上または床面にある日用品を解析してください。

対象:
- 3〜35cm程度の移動可能な日用品
- 最大5個
- 外観で互いに区別できる物体

各対象について返す情報:
- この応答内で一意なID
- 短い日本語表示名
- 対応付けに使える具体的な外観特徴
- 画像全体を0〜1000に正規化したBounding box
- 元の向きへ戻す必要があるか
- none、bilateral、rotationalのいずれかの対称性

透明、鏡面、強い反射、布、液体、ケーブル束、固定物、同じ外観の複数物体は対象へ含めず、excludedCandidatesへ理由とともに入れてください。
Bounding boxは見えている部分ではなく物体全体の範囲を、推測せず確認可能な範囲で返してください。
対象が6個以上ある場合も先頭5個へ切り詰めず、対象候補をすべて返してください。`
